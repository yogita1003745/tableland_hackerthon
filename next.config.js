/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    // Add your custom configurations here
    reactStrictMode: true, // Enable React Strict Mode
    // You can add other configurations like webpack, redirects, etc.
  };

// module.exports = nextConfig
